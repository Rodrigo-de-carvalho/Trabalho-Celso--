# Porta em que o frontend (Create React App) vai rodar.
# Por padrão o CRA usa a porta 3000; aqui trocamos para 5173, que é a porta
# que o backend libera no CORS (backend/src/config/cors.ts).
PORT=5173
